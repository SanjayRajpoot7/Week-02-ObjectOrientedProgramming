package problemStatements.rideHailingApplication;

public interface GPS {
        String getCurrentLocation();  // Get current location of the vehicle
        void updateLocation(String newLocation);  // Update vehicle's current location
    }

